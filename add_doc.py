from app.rag.rag import add_document
from app.rag.rag import search
print(search("framework"))

add_document("Django is a Python web framework.")
add_document("Akash built an AI assistant project.")

